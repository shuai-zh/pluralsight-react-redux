import React from "react";
import PropTypes from "prop-types";

const NotFoundPage = props => {
  return <div className="alert alert-danger">404, Page not found!</div>;
};

NotFoundPage.propTypes = {};

export default NotFoundPage;
